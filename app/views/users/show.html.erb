<p style="color: green">
	<%= notice %>
</p>

<%= render @user %>

<div>
	<%= link_to "Editeaza", edit_user_path(@user) %> |
	<%= link_to "Inapoi la users", users_path %>

	<%= button_to "Sterge user", @user, method: :delete %>
	<% if @user.present? && (@rezervares.any? && (@rezervares.first.user == current_user || current_user.admin?)) %>
	<div class="col-md-6">
	  <h3> Rezervari (<%= @rezervares.count%>)</h3>
	  <%= render @rezervares %>
	  <%= render partial: 'rezervares/pagination' %>
	  
	</div>
  <% end %>   

</div>
